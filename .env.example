APP_ENV=local
LOG_LEVEL=INFO

# Optional — toggles the reported backend mode; orchestration runs offline either way.
FOUNDRY_PROJECT_ENDPOINT=
FOUNDRY_MODEL_NAME=gpt-4o

# Loan amount above which a run pauses for human approval (HITL).
HITL_AMOUNT_THRESHOLD=250000
